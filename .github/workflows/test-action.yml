name: Test Action

on:
  #push:
  #  branches:
  #   - main
    
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Action
        uses: ./
        with:
          solutionPath: "C:\\temp\\temp.sln"
          targetNetId: "192.168.4.1.1.1"
          targetPlatform: "TwinCAT RT (x64)"
           
